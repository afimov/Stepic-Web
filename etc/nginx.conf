server {
	location ^~ /uploads/ {
		root /home/evil/web;
	}

	location ~* ^.+\.(jpg|jpeg|gif|png|js|css)$ {
		root /home/evil/web/public;
	}

	location /hello/ {
		proxy_pass http://127.0.0.1:8080;
	}
}

